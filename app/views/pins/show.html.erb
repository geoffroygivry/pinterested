<p id="notice"><%= notice %></p>

<p>
  <%= image_tag @pin.image.url(:original) %> </br>
  <strong>Description:</strong>
  <%= @pin.description %><br/>
  <%= @pin.user.name %>
</p>

<% if @pin.user == current_user %>
  <%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>
